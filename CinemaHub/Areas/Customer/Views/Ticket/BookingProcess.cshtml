<div class="container mt-5" style="min-height:640px">
    <div class="jumbotron text-center">
        <h3 class="display-4 text-success">Payment Successful!</h3>
        <p class="lead">Your payment was successful. Thank you for booking with us.</p>
        <p class="lead">We have sent you an email for your order. Please check your email to get more details. <a href="https://mail.google.com/mail/u/0/#inbox" target="_blank">Click here</a> to check your Gmail inbox.</p>

        <!-- Button to navigate to the Booked Tickets page -->
        <a href="/Customer/Ticket/BookedTickets" class="btn btn-primary btn-lg">View Your Tickets</a>

        <!-- Countdown timer with redirection to home page -->
        <div id="countdown" class="mt-3 text-muted"><a href="/Customer">Back to Home</a> in <span id="timer">60</span> seconds</div>
    </div>
</div>

<script>
    // Countdown timer function
    function countdownTimer(duration, display) {
        var timer = duration, minutes, seconds;
        setInterval(function () {
            minutes = parseInt(timer / 60, 10);
            seconds = parseInt(timer % 60, 10);

            minutes = minutes < 10 ? "0" + minutes : minutes;
            seconds = seconds < 10 ? "0" + seconds : seconds;

            display.textContent = minutes + ":" + seconds;

            if (--timer < 0) {
                timer = duration;
                // Redirect to the home page after the countdown ends
                window.location.href = "/Customer";
            }
        }, 1000);
    }

    // Start the countdown when the page loads
    window.onload = function () {
        var duration = 59; // Duration in seconds
        var display = document.getElementById("timer");
        countdownTimer(duration, display);
    };
</script>

<style>
    /* Additional CSS for styling */
    .jumbotron {
        background-color: #f8f9fa;
        border-radius: 10px;
        padding: 30px;
    }

    .btn-primary {
        background-color: #007bff;
        border-color: #007bff;
    }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
        }
</style>
